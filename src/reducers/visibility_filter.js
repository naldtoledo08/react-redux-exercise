export default function visibility_filter (state = 0, payload) {
	
	//console.log('--- Visibility filter Toggle');
  switch (payload.type) {
   // case 'show_all':
      //return state 
    default:
      return state
  }
}